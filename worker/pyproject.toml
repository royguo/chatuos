[project]
name = "chatuos-worker"
version = "0.1.0"
description = "Contributor-side Codex worker for ChatUOS"
requires-python = ">=3.8"
dependencies = []

[project.scripts]
chatuos-worker = "codex_share_worker.__main__:main"
